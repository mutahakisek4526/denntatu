<UserControl x:Class="AacV1.Views.MailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="宛先" Width="80" VerticalAlignment="Center" />
            <TextBox Text="{Binding To, UpdateSourceTrigger=PropertyChanged}" Width="400" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="件名" Width="80" VerticalAlignment="Center" />
            <TextBox Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged}" Width="400" />
        </StackPanel>
        <TextBox Grid.Row="2" Text="{Binding Body, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="送信" Command="{Binding SendCommand}" />
            <TextBlock Text="{Binding Status}" Margin="12,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
